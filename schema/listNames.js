const prefix = 'KsApp' // this should be DefinedPascalCase !!!

const ArticleListName = `${prefix}Article`
const CommentListName = `${prefix}Comment`
const LikeCommentListName = `${prefix}LikeComment`
const LikeUserListName = `${prefix}LikeUser`
const UserListName = `${prefix}User`

module.exports = {
    ArticleListName,
    CommentListName,
    LikeCommentListName,
    LikeUserListName,
    UserListName
}